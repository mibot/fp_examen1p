def consumo_planilla(lecturaActual, lecturaAnterior, costoKw):
    """
    Calcula el valor a pagar por el consumo de electricidad.

    Args:
    lecturaActual (float): La lectura actual del medidor.
    lecturaAnterior (float): La lectura anterior del medidor.
    costoKw (float): El costo por kilovatio establecido.

    Returns:
    float: El valor a pagar por el consumo.
    """

    # Tu codigo aquí

    # return valorPagar

# Ejemplo de uso
# if __name__ == '__main__':
    # lactual = float(input("lectura actual: "))
    # lanterior = float(input("lectura anterior: "))
    # costo = float(input("costo: "))
    # print(consumo_planilla(lactual,lanterior,costo))

